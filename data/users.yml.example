# oops 用户配置文件
# 使用 hash-password 子命令生成 bcrypt 密码 hash：
#   oops hash-password
# 然后把输出的 hash 填到 password 字段。
#
# 此文件支持热加载：修改后无需重启 oops。

users:
  admin:
    name: "管理员"
    password: "$2a$10$..."

  # 如需添加更多用户，在此追加：
  # operator:
  #   name: "运维人员"
  #   password: "$2a$10$..."
