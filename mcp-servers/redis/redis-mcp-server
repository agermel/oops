#!/usr/bin/env bash
set -euo pipefail

root="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
if [[ -n "${OOPS_MCP_VENV_ROOT:-}" ]]; then
	export UV_PROJECT_ENVIRONMENT="$OOPS_MCP_VENV_ROOT/redis"
fi

exec uv run --directory "$root" --locked python "$root/entrypoint.py" "$@"
