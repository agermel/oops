#!/bin/bash
# Nacos MCP Router wrapper
# Uses the official Nacos MCP Router package.
# Configure via env: NACOS_ADDR, NACOS_USERNAME, NACOS_PASSWORD, NACOS_NAMESPACE.
# uvx downloads and caches on first run; subsequent runs are fast.
exec uvx --from nacos-mcp-router@latest nacos-mcp-router "$@"
